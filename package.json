{
  "name": "rate-pulse",
  "version": "1.0.0",
  "description": "Rate.Pulse — Mortgage Rate Dashboard",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000",
    "start": "npx serve public -l 3000",
    "build": "echo 'Static site — no build step required'"
  }
}
